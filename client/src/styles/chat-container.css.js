import {css} from 'lit';

export default css`
  :host {
    flex-grow: 1;
  }
  .chat-container {
    box-shadow: 0px 0px 16px rgba(172, 181, 212, 0.3);
    border-radius: 8px;
    max-width: 80%;
  }
`;
